# Инструкция по деплою Samkraft в Cloudflare Pages

## Шаг 1: Создание базы данных D1

1. **Войдите в свой аккаунт Cloudflare** и откройте терминал на своём компьютере

2. **Создайте базу данных D1:**
   ```bash
   npx wrangler d1 create samkraft-db
   ```

3. **Скопируйте `database_id`** из вывода команды. Он будет выглядеть примерно так:
   ```
   [[d1_databases]]
   binding = "DB"
   database_name = "samkraft-db"
   database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
   ```

4. **Обновите файл `wrangler.jsonc`** в вашем репозитории:
   
   Откройте файл `wrangler.jsonc` и замените `placeholder-replace-after-creating-database` на ваш реальный `database_id`:
   
   ```jsonc
   {
     "$schema": "node_modules/wrangler/config-schema.json",
     "name": "samkraft",
     "compatibility_date": "2026-02-13",
     "pages_build_output_dir": "./dist",
     "compatibility_flags": [
       "nodejs_compat"
     ],
     "d1_databases": [
       {
         "binding": "DB",
         "database_name": "samkraft-db",
         "database_id": "ВАШ_НАСТОЯЩИЙ_DATABASE_ID_ЗДЕСЬ"
       }
     ]
   }
   ```

5. **Примените миграции к базе данных:**
   ```bash
   npx wrangler d1 migrations apply samkraft-db --remote
   ```
   
   Эта команда создаст все таблицы в вашей production базе данных.

## Шаг 2: Создание проекта Cloudflare Pages

### Вариант A: Через веб-интерфейс (Рекомендуется)

1. **Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)**

2. **Перейдите в раздел "Pages"** в левом меню

3. **Нажмите "Create a project"** → **"Connect to Git"**

4. **Выберите GitHub** и авторизуйте Cloudflare для доступа к вашим репозиториям

5. **Выберите репозиторий** `Samkraft`

6. **Настройте параметры сборки:**
   - **Project name:** `samkraft` (или другое имя по желанию)
   - **Production branch:** `main`
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`

7. **Свяжите базу данных D1:**
   - В настройках проекта перейдите в раздел **Settings** → **Functions**
   - Прокрутите до секции **D1 database bindings**
   - Нажмите **Add binding**
   - **Variable name:** `DB`
   - **D1 database:** выберите `samkraft-db` из списка
   - Сохраните

8. **Нажмите "Save and Deploy"**

9. **Дождитесь завершения деплоя** (обычно 2-3 минуты)

10. **Получите URL:** После успешного деплоя вы получите URL вида:
    ```
    https://samkraft.pages.dev
    ```

### Вариант B: Через командную строку

1. **Создайте проект Pages:**
   ```bash
   npx wrangler pages project create samkraft --production-branch main
   ```

2. **Соберите проект:**
   ```bash
   npm run build
   ```

3. **Задеплойте:**
   ```bash
   npx wrangler pages deploy dist --project-name samkraft
   ```

4. **Свяжите D1 базу данных:**
   ```bash
   npx wrangler pages deployment create --project-name=samkraft --branch=main
   ```
   
   Затем в веб-интерфейсе добавьте binding для базы данных (см. Вариант A, шаг 7).

## Шаг 3: Настройка custom domain (Опционально)

1. В Cloudflare Dashboard перейдите в **Pages** → **ваш проект** → **Custom domains**

2. Нажмите **Set up a custom domain**

3. Введите ваш домен (например, `samkraft.se`)

4. Следуйте инструкциям для настройки DNS записей

## Шаг 4: Проверка работоспособности

1. **Откройте ваш сайт** в браузере (используйте URL из шага 2)

2. **Проверьте API:**
   ```bash
   curl https://your-project.pages.dev/api/health
   ```
   
   Должен вернуть:
   ```json
   {
     "status": "ok",
     "timestamp": "...",
     "service": "Samkraft API"
   }
   ```

3. **Проверьте список проектов:**
   ```bash
   curl https://your-project.pages.dev/api/projects
   ```

## Шаг 5: Автоматический деплой при push

После настройки через веб-интерфейс (Вариант A):

1. **Каждый push в ветку `main`** автоматически запускает новый деплой

2. **Pull request** создают preview деплои с уникальными URL

3. **Мониторинг деплоев** доступен в разделе **Deployments** вашего проекта

## Устранение неполадок

### Ошибка: "Database not found"

**Причина:** D1 binding не настроен правильно

**Решение:**
1. Убедитесь, что в `wrangler.jsonc` указан правильный `database_id`
2. Проверьте, что в настройках Pages добавлен binding с именем `DB`
3. Пересоберите и задеплойте проект

### Ошибка: "Table does not exist"

**Причина:** Миграции не применены к production базе

**Решение:**
```bash
npx wrangler d1 migrations apply samkraft-db --remote
```

### Ошибка при билде: "Module not found"

**Причина:** Не установлены зависимости

**Решение:**
```bash
npm install
npm run build
```

### API возвращает 500 ошибку

**Причина:** Проблема с базой данных или кодом

**Решение:**
1. Проверьте логи в Cloudflare Dashboard → Pages → ваш проект → Deployments → View details
2. Убедитесь, что миграции применены
3. Проверьте, что binding `DB` настроен правильно

## Обновление приложения

1. **Внесите изменения** в код локально

2. **Закоммитьте и запушьте:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

3. **Cloudflare автоматически задеплоит** новую версию

## Откат к предыдущей версии

1. В Cloudflare Dashboard перейдите в **Pages** → **ваш проект** → **Deployments**

2. Найдите предыдущий успешный деплой

3. Нажмите **...** → **Rollback to this deployment**

## Полезные команды

```bash
# Просмотреть текущую конфигурацию
npx wrangler pages project list

# Просмотреть деплои
npx wrangler pages deployment list --project-name=samkraft

# Удалить проект (осторожно!)
npx wrangler pages project delete samkraft

# Просмотреть логи D1
npx wrangler d1 execute samkraft-db --remote --command="SELECT * FROM projects LIMIT 5"
```

## Дополнительные ресурсы

- [Cloudflare Pages Documentation](https://developers.cloudflare.com/pages/)
- [Cloudflare D1 Documentation](https://developers.cloudflare.com/d1/)
- [Hono Documentation](https://hono.dev/)
- [Wrangler CLI Documentation](https://developers.cloudflare.com/workers/wrangler/)

---

**Готово!** Ваше приложение Samkraft теперь задеплоено на Cloudflare Pages и доступно по всему миру через edge network Cloudflare. 🎉
